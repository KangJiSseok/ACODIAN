-- Flyway execution source of truth
-- Purpose: PostgreSQL extension baseline for AX-WMS schema bootstrap.
-- Note: This file is part of the executable migration set applied by Flyway and the PostgreSQL bootstrap used for jOOQ code generation.

CREATE EXTENSION IF NOT EXISTS vector;
